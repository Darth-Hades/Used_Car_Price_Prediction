# Used_Car_Price_Prediction
Used Car Price Prediction is a machine learning project that estimates the resale value of cars based on features like brand, year, mileage, fuel type, and transmission. It uses models like Linear Regression and Random Forest, with EDA and preprocessing, and is deployed via Streamlit for real-time predictions.
